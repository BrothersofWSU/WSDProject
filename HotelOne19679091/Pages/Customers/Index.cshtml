@page
@model HotelOne19679091.Pages.Customers.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer[0].lname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer[0].fname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer[0].postCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Customer) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.postCode)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.email">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.email">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.email">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
